{
  "description": "checkJs coverage for Node and serverless production modules.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "checkJs": true
  },
  "include": [
    "api/**/*.js",
    "lib/**/*.js",
    "server/**/*.js",
    "dev-server.js"
  ]
}
