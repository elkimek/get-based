# Cycle import fixtures

The `cycle-*` files in this directory are synthetic, anonymized parser fixtures.
They exercise the documented container types and the field variants supported by
getbased, but they are not vendor-issued exports.

Clue and Natural Cycles remain marked for manual review in the import preview
until an anonymized, vendor-issued export has been validated and recorded.
